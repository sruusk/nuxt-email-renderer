<script>
import { defineComponent, h } from "vue";
export default defineComponent({
  name: "EColumn",
  setup(_, { slots }) {
    return () => {
      return h(
        "td",
        {
          role: "presentation"
        },
        slots.default?.()
      );
    };
  }
});
</script>
